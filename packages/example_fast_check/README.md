# example-fast-check: Example usage of reify-ts for fast-checking APIs

Use reify-ts to generate sample values for a given type, test a function over its whole definition domain, and go beyond &ldquo;foo-bar&rdquo; tests.

## Usage

See the [README](../../README.md) at the root of the monorepo.
